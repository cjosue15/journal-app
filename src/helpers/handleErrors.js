// const handleErrors = (values) => {
//     const errors = Object.keys(values).map((value) => {
//         return { name: value, valid: values[value] && values[value].trim().length > 0 ? true : false };
//     });

//     const valid = errors.find((item) => !item.valid);

//     return { inValid: !valid ? false : true, errors };
// };
